
<ul class="medium-horizontal vertical dropdown menu">
    <!-- Loop through the navigation items -->
    {{#foreach navigation}}
    <li class="menu-item nav-{{slug}}{{#if current}} active{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></li>
    {{/foreach}}
    <!-- End the loop -->
</ul>